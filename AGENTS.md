# คู่มือการปฏิบัติการสำหรับ AI Agents และนักพัฒนา (AGENTS.md)

## โครงการ: MCU Council RSVP (ระบบตอบรับเข้าร่วมประชุมสภามหาวิทยาลัย)
**หน่วยงาน:** สำนักงานสภามหาวิทยาลัย มหาวิทยาลัยมหาจุฬาลงกรณราชวิทยาลัย (มจร.)  
**บริบทของระบบ:** Web Application แบบ Full-stack สำหรับบริหารการประชุมสภาฯ, การตอบรับ (Onsite/Online/Hybrid), การเช็กชื่อ, ตรวจสอบองค์ประชุม, และออกรายงานทางการ

---

## 1. กฎเหล็กทางธุรกิจที่ห้ามละเมิดเด็ดขาด (Cardinal Business Invariants)

เมื่อทำการแก้ไข พัฒนาเพิ่มเติม หรือ refactor โค้ดในโปรเจกต์นี้ AI Agents และนักพัฒนา **ต้องปฏิบัติตามกฎ 7 ข้อนี้อย่างเคร่งครัดโดยไม่มีข้อยกเว้น**:

### 1. กฎการจัดเก็บและการแสดงผลวันและเวลา (Date/Time Invariant)
- **การจัดเก็บ (Storage / State / DB)**: ต้องจัดเก็บในรูปแบบมาตรฐานสากล **ISO 8601** (`YYYY-MM-DD` หรือ `YYYY-MM-DDTHH:mm:ssZ`) เสมอ ห้ามนำปี พ.ศ. ไปบันทึกลงในฐานข้อมูลหรือฟิลด์วันที่โดยตรง
- **การแสดงผล (Display / UI / Reports)**: ต้องแสดงผลเป็น **ปฏิทินภาษาไทยและปี พ.ศ. เสมอ** (เช่น "๒๔ กันยายน ๒๕๖๙" หรือ "24 ก.ย. 2569") โดยเรียกใช้ฟังก์ชันจาก `src/utils/thaiDate.ts` เสมอ ห้าม hardcode วันที่ภาษาอังกฤษ

### 2. กฎการนับองค์ประชุมของผู้แทน (Quorum Delegation Invariant)
- **ห้ามนับผู้แทนเป็นองค์ประชุมโดยอัตโนมัติเป็นอันขาด**: ค่าฟิลด์ `canCountAsQuorum` ใน `DelegateRequest` ต้องมีค่าเริ่มต้นเป็น `false` เสมอ
- ผู้แทนจะถูกนับเป็นองค์ประชุมได้ก็ต่อเมื่อ **เลขานุการสภามหาวิทยาลัย (`secretary`)** เป็นผู้พิจารณาอนุมัติและทำเครื่องหมายยินยอมให้มีสิทธิ์นับองค์ประชุมในหน้า `/approvals` เท่านั้น

### 3. กฎสถานที่จัดการประชุมเริ่มต้น (Default Venue Invariant)
- สถานที่จัดการประชุมเริ่มต้นของระบบต้องถูกเติมอัตโนมัติเป็น:  
  *“ห้องประชุม 401 ชั้น 4 อาคารพระธรรมวชิรคุณาธาร (โกศล มหาวีโร) อาคารสำนักงานอธิการบดี มหาวิทยาลัยมหาจุฬาลงกรณราชวิทยาลัย ตำบลลำไทร อำเภอวังน้อย จังหวัดพระนครศรีอยุธยา”*
- **ช่องสถานที่ต้องแก้ไขได้เสมอ** และต้องมีข้อความกำกับ: *“ระบบกำหนดสถานที่มาตรฐานให้อัตโนมัติ สามารถแก้ไขได้ในกรณีเปลี่ยนสถานที่ประชุม”*

### 4. กฎข้อมูลตัวอย่างสมมุติและห้ามล็อกข้อมูล (Data Mutability Invariant)
- ข้อมูลการประชุม กรรมการ และการตอบรับทั้งหมดเป็น **ข้อมูลสมมุติ (Fictitious Data)** ตามบริบทของ มจร. (พระเถระ, ผู้บริหาร, ผู้ทรงคุณวุฒิ) ห้ามใช้ข้อมูลส่วนบุคคลจริง (Non-PII)
- **ห้ามล็อกข้อมูลการประชุมตัวอย่างไว้ในระบบ**: ผู้ใช้งานต้องสามารถ สร้าง, แก้ไข, ลบ, คัดลอก (Duplicate), และเปิด/ปิดรับตอบรับได้ทุกรายการอย่างอิสระ พร้อมทั้งมีปุ่ม Reset Seed Data ในหน้าตั้งค่า

### 5. กฎความสมบูรณ์ในการพิจารณาคำขอ (Approval Validation Invariant)
- เมื่อเลขานุการสภาฯ เลือก **"ไม่อนุมัติ (rejected)"** หรือ **"ขอข้อมูลเพิ่มเติม (info_requested)"** สำหรับคำขอลาหรือคำขอมอบหมายผู้แทน **ระบบต้องบังคับให้กรอกหมายเหตุ/เหตุผลเสมอ** ห้ามปล่อยให้เป็นค่าว่าง

### 6. กฎการตรวจสอบวันเวลาปิดรับตอบรับ (Deadline Validation Invariant)
- วันและเวลาปิดรับการตอบรับ (`rsvpDeadline`) **ต้องอยู่ก่อน** วันและเวลาเริ่มการประชุม (`meetingDate` + `startTime`) เสมอ ระบบต้องมี Validation ดักจับและปฏิเสธการบันทึกหากเงื่อนไขไม่ถูกต้อง

### 7. กฎความปลอดภัยและความเป็นส่วนตัว (PDPA & QR Code Invariant)
- ในภาพ QR Code สำหรับเช็กชื่อวันประชุม **ห้ามบรรจุข้อมูลส่วนบุคคลที่เปิดเผยได้ (เช่น ชื่อ-นามสกุล, เบอร์โทร, เลขบัตรประชาชน)** แต่ให้บรรจุเฉพาะรหัสอ้างอิงคลุมเครือ เช่น `MCU-RSVP:[RefCode]:[InviteeId]` เพื่อความปลอดภัยตามมาตรฐาน PDPA

---

## 2. โครงสร้างไฟล์และแผนที่นำทางโค้ดเบส (Codebase Navigation)

| เส้นทางไฟล์สำคัญ | หน้าที่และความรับผิดชอบ |
|---|---|
| `src/context/DataContext.tsx` | ศูนย์กลาง State และ Reactive Store ของระบบ จัดการ CRUD ประชุม, สมาชิก, ผลตอบรับ, เช็กชื่อ, องค์ประชุม, และ LocalStorage |
| `src/context/AuthContext.tsx` | จัดการ Session ผู้ใช้, สิทธิ์ RBAC 6 ระดับ, และ Quick Role Switcher |
| `src/utils/thaiDate.ts` | เครื่องยนต์แปลงวันและเวลา ISO 8601 <-> พ.ศ., แปลงวันที่ทางการไทย, ตัวเลือกปี พ.ศ. |
| `src/types/index.ts` | TypeScript Interfaces ครอบคลุมเอนทิตีทั้งหมดของระบบ |
| `src/data/seedData.ts` | ข้อมูลตัวอย่างสมมุติภาษาไทย มจร. (คณะกรรมการ, สมาชิก, การประชุม Onsite/Online/Hybrid) |
| `supabase/schema.sql` | สคีมาฐานข้อมูล PostgreSQL/Supabase 17 ตาราง พร้อม RLS และ Indexes |
| `supabase/seed.sql` | ข้อมูลจำลอง SQL สำหรับรันใน Supabase/PostgreSQL |
| `src/components/layout/Navbar.tsx` | Header บาร์ พร้อมตราธรรมจักร มจร., Role Switcher, และตัวแจ้งเตือน |
| `src/components/layout/Sidebar.tsx` | แถบเมนูด้านข้าง รองรับ 2 โครงสร้างเมนู (Staff/Admin และ Member) |
| `src/components/ui/StatusBadge.tsx` | ป้ายสถานะ 5 สีมาตรฐานตามข้อกำหนด (เข้าร่วม, ออนไลน์, รอตรวจ, ลา, ยังไม่ตอบ) |

---

## 3. รูปแบบคำสั่งและการทำงานทั่วไป (Common Workflows & Commands)

### 3.1 การรันระบบและการทดสอบ
```bash
# รันโหมด Development (Hot Reloading)
npm run dev

# รันการทดสอบ Unit Tests (Vitest)
npm test

# ตรวจสอบ Type Check และทดสอบ Build สำหรับ Production
npm run build

# พรีวิว Production Build
npm run preview
```

### 3.2 การทดสอบสลับบทบาทผู้ใช้ (Testing Role Switching)
- ไม่จำเป็นต้องสร้างบัญชีใหม่สำหรับการทดสอบแต่ละบทบาท
- บนแถบ Header ด้านขวาบน จะมีปุ่ม **"บทบาท: [Role]"** ให้คลิกเพื่อสลับระหว่าง `super_admin`, `staff`, `secretary`, `chairperson`, `member`, `delegate` ได้ทันที
- ในหน้า Login (`/login`) มีแผง **Quick Demo Roles** ให้คลิกเพื่อเข้าสู่ระบบตามบทบาทที่ต้องการได้ใน 1 คลิก

### 3.3 การรีเซ็ตข้อมูลตัวอย่างกลับเป็นค่าเริ่มต้น
- หากมีการทดสอบเพิ่ม ลบ หรือแก้ไขข้อมูลจนต้องการคืนค่าตั้งต้น ให้ไปที่เมนู **"ตั้งค่าระบบ" (`/settings`)** แล้วกดปุ่มสีแดง **"รีเซ็ตข้อมูลตัวอย่าง (Reset Seed)"** ที่มุมบนขวา

---

## 4. มาตรฐานการเขียนโค้ดและสไตล์ (Coding Standards & Best Practices)

1. **Accessibility (a11y)**:
   - ปุ่มและ Input ต้องมี label ชัดเจน, Color Contrast ต้องได้มาตรฐาน WCAG AA, ปุ่มกดบนอุปกรณ์มือถือต้องมีขนาดไม่น้อยกว่า 40x40px
2. **State Management**:
   - ห้ามแก้ไข State ใน `DataContext` โดยตรง (Direct Mutation) ต้องผ่านฟังก์ชัน Dispatcher เช่น `submitRSVP`, `checkInInvitee`, `reviewDelegateRequest` เพื่อให้มีการบันทึก Audit Log และการแจ้งเตือนสม่ำเสมอ
3. **Empty, Loading, Error States**:
   - ทุกหน้าจอที่มีการเรียกดูหรือกรองข้อมูล ต้องมีส่วนประกอบ `EmptyState`, `LoadingState`, หรือ `ErrorState` รองรับเสมอ
4. **Tailwind Styling**:
   - ใช้คลาสสีตาม Theme มจร. เช่น `text-mcu-primary` (`#4B1F5E`), `bg-mcu-primary`, `text-mcu-gold` (`#C8A54B`), และ `bg-mcu-bg` (`#F7F7F8`)
